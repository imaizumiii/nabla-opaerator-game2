# 潜在的な問題点とエッジケース分析

## 分析対象
- `app/components/GameBoard.tsx`
- `app/hooks/useGameState.ts`

---

## 1. GameBoard.tsx の問題点

### 1.1 カード選択ロジックの複雑性
**場所:** `handleHandCardClick` (19-81行)

**問題点:**
- カード選択ロジックが複雑で、状態遷移が分かりにくい
- 乗算・除算のオペランド選択と通常選択が混在している

**潜在的なバグ:**
- 乗算カード選択後、関数カードを選択→別の関数カードを選択した場合、オペランドが入れ替わるが、視覚的フィードバックが不十分
- 微分/積分スタック時に、他の演算子が選択されている場合の挙動が曖昧

**検証項目:**
- [x] 乗算カード選択後、関数カードを2回クリック → オペランドが正しく入れ替わるか
- [x] 微分スタック中に乗算カードを選択 → 微分は解除されるか

### 1.2 AoE演算子のターゲット指定
**場所:** `executeFieldAction` (94-174行)

**問題点:**
```143:145:app/components/GameBoard.tsx
            applyOperator(operators, null, aoETargetOwnerId, null, functions[0]); // functions[0] is undefined usually
```

**潜在的なバグ:**
- AoE演算子の場合、`functions[0]`は通常`undefined`だが、コメントで「usually」とあり、確実性がない
- ナブラ/ラプラシアンは`functions`を必要としないが、パラメータとして渡している

**検証項目:**
- [ ] ナブラ/ラプラシアンに関数カードが誤って渡されても問題ないか
- [ ] AoE演算子選択時に関数カードが選択されていた場合の挙動

### 1.3 フィールドエリアクリックとカードクリックの競合
**場所:** `handleFieldAreaClick` (88-92行), `handleFieldCardClick` (83-86行)

**問題点:**
- カードクリックとフィールドエリアクリックが競合する可能性がある
- `e.target === e.currentTarget`による判定は、カード内の子要素クリック時に不正確になる可能性がある

**検証項目:**
- [ ] カードの子要素（ラベルなど）をクリックした場合、カードクリックとして扱われるか
- [ ] カードクリック時にフィールドエリアクリックが発火しないか

### 1.4 フィールド上限チェックの二重検証
**場所:** `executeFieldAction` (108-112行), `deployFunction` (179-183行)

**問題点:**
- `GameBoard.tsx`と`useGameState.ts`の両方でフィールド上限チェックを実施
- 冗長だが、両方でチェックする意図があるか不明

**検証項目:**
- [ ] `GameBoard.tsx`のチェックで弾かれた場合、`deployFunction`は呼ばれないか
- [ ] 将来的に`deployFunction`を直接呼ぶ可能性がある場合、チェックが漏れないか

---

## 2. useGameState.ts の問題点

### 2.1 状態更新の競合（Race Condition）
**場所:** `applyOperator`, `deployFunction` (非同期処理)

**問題点:**
- `stateRef.current`を使用して最新状態を取得しているが、複数の操作が同時に行われる可能性がある
- 非同期処理中に状態が変更された場合、古い状態で計算が行われる可能性がある

**検証項目:**
- [ ] 連続してカードをクリックした場合、状態が正しく更新されるか
- [ ] 非同期処理中に別の操作が行われた場合、競合しないか

### 2.2 AoE演算子のエラーハンドリング
**場所:** `applyOperator` (240-265行)

**問題点:**
```261:264:app/hooks/useGameState.ts
        } catch (e: any) {
            console.error("全体攻撃計算エラー:", e);
            alert(`無効な計算です。\n詳細: ${e.message}`);
            return;
```

**潜在的なバグ:**
- AoE演算子適用中にエラーが発生した場合、`return`のみで状態が更新されない
- ただし、`try`の前に状態を変更していないので、問題ない可能性が高い

**検証項目:**
- [ ] AoE演算子適用中にAPIエラーが発生 → 状態が変更されず、ゲームが継続するか

### 2.3 ゼロ関数の処理順序
**場所:** `applyOperator` (417-435行)

**問題点:**
- `isZero: true`の場合、カードを削除してから線形従属チェックを行う
- 削除後の線形従属チェックは必要ない可能性があるが、念のため実行している

**検証項目:**
- [ ] ゼロ関数削除後の線形従属チェックが不要かどうか
- [ ] パフォーマンスへの影響

### 2.4 線形従属チェックのエラー処理
**場所:** `resolveLinearDependence` (134-155行), `MathEngine.checkLinearDependence` (46-70行)

**問題点:**
```64:69:app/lib/math-engine.ts
    } catch (e) {
      console.error("Linear Dependence API Error:", e);
      // エラー時は安全側に倒して重複なしとする（あるいは再スロー）
      // ここではゲーム進行を止めないため重複なしとするが、ログは出す
      return { isDependent: false, dependentIndices: [] };
    }
```

**潜在的なバグ:**
- APIエラー時、重複なしとして扱うため、実際には重複がある場合でも削除されない
- ゲーム進行は止まらないが、バランスが崩れる可能性がある

**検証項目:**
- [ ] APIエラー時のフォールバック動作が適切か
- [ ] ユーザーへの通知が必要か

### 2.5 デッキ枯渇時の手札補充
**場所:** `deployFunction` (204-210行), `applyOperator` (320-327行, 471-478行)

**問題点:**
- デッキが空の場合、`while`ループは実行されるが、カードが追加されない
- これは意図通りだが、デッキが枯渇した場合の処理が明示的でない

**検証項目:**
- [ ] デッキ枯渇時、手札が7枚未満でも問題なくゲームが継続するか
- [ ] デッキ枯渇時のユーザー通知が必要か

### 2.6 カードIDの生成
**場所:** 複数箇所（`deployFunction`, `applyOperator`, `drawCard`など）

**問題点:**
- カードIDの生成に`Date.now()`を使用しているが、同一ミリ秒内に複数のカードが生成される可能性がある
- 現在は追加のカウンターを使用しているが、完全に一意である保証はない

**検証項目:**
- [ ] カードIDが重複しないか
- [ ] カードIDのフォーマットが一貫しているか

### 2.7 ターン終了のタイミング
**場所:** `deployFunction` (199-202行), `applyOperator` (315-327行, 466-479行)

**問題点:**
- `deployFunction`と`applyOperator`の両方でターン終了処理を実装
- コードの重複があり、保守性が低い

**検証項目:**
- [ ] ターン終了処理が一貫しているか
- [ ] リファクタリングが必要か

---

## 3. エッジケースの検証

### 3.1 空フィールドでのAoE演算子
**問題点:**
- フィールドが空の状態でナブラ/ラプラシアンを適用した場合の挙動

**期待動作:**
- 何も起こらない（またはエラーメッセージ）
- カードは消費されない（または消費される？）

**検証項目:**
- [ ] 空フィールドでAoE演算子を適用 → エラー処理または警告表示

### 3.2 手札が空の状態
**問題点:**
- 手札が0枚の状態での操作

**期待動作:**
- UIが正しく表示される
- カード選択ができない

**検証項目:**
- [ ] 手札が空の状態でゲームが継続するか
- [ ] UIが正しく表示されるか

### 3.3 フィールドが1枚の状態で線形従属チェック
**問題点:**
```134:136:app/hooks/useGameState.ts
  const resolveLinearDependence = async (field: FunctionCard[]): Promise<FunctionCard[]> => {
    if (field.length <= 1) return field;
```

- 1枚以下ならチェックをスキップしているが、0枚の場合は明示的でない

**検証項目:**
- [ ] フィールドが0枚の状態 → チェックがスキップされるか

### 3.4 同時に複数の演算子が適用される可能性
**問題点:**
- ユーザーが高速にクリックした場合、複数の操作が同時に実行される可能性

**期待動作:**
- 1つの操作が完了するまで、次の操作をブロックする

**検証項目:**
- [ ] 連続クリック時の挙動
- [ ] ローディング状態の表示が必要か

### 3.5 勝利判定のタイミング
**問題点:**
- `deployFunction`と`applyOperator`の両方で勝利判定を実装
- 勝利後の処理が一貫しているか

**検証項目:**
- [ ] 勝利後、操作が無効化されるか
- [ ] 勝利後のターン切り替えが実行されないか

---

## 4. パフォーマンスの問題

### 4.1 線形従属チェックの呼び出し頻度
**問題点:**
- Deploy後、演算子適用後、AoE演算子適用後など、複数箇所で線形従属チェックを実行
- API呼び出しが頻繁になる可能性がある

**検証項目:**
- [ ] API呼び出しの頻度が適切か
- [ ] パフォーマンスへの影響

### 4.2 状態更新の頻度
**問題点:**
- `setGameState`が頻繁に呼ばれる可能性がある
- 再レンダリングの頻度が高くなる可能性がある

**検証項目:**
- [ ] 再レンダリングが過度に発生していないか
- [ ] `useMemo`や`useCallback`の最適化が必要か

---

## 5. UI/UXの問題

### 5.1 メッセージインジケータの表示
**場所:** `getActionMessage` (177-215行)

**問題点:**
- メッセージが英語で固定されている
- カード選択状態に応じてメッセージが変更されるが、分かりやすさに課題がある可能性

**検証項目:**
- [ ] メッセージがユーザーにとって分かりやすいか
- [ ] 日本語化が必要か

### 5.2 ローディング状態の表示
**問題点:**
- 非同期処理中（API呼び出し中）にローディング状態が表示されない
- ユーザーが操作を待つ必要があるが、フィードバックが不十分

**検証項目:**
- [ ] 非同期処理中のローディング表示が必要か
- [ ] ユーザーが操作を繰り返した場合の挙動

---

## 検証優先度

### 高優先度
1. AoE演算子のターゲット指定（2.2）
2. 状態更新の競合（2.1）
3. 勝利判定のタイミング（3.5）

### 中優先度
4. カード選択ロジックの複雑性（1.1）
5. フィールドエリアクリックとカードクリックの競合（1.3）
6. 線形従属チェックのエラー処理（2.4）

### 低優先度
7. UI/UXの問題（5章）
8. パフォーマンスの問題（4章）
9. コードのリファクタリング（2.7）

---

## 推奨される修正

1. **状態更新の競合対策:** 操作中フラグを追加し、同時操作を防止
2. **ローディング状態の表示:** 非同期処理中にローディングインジケータを表示
3. **エラーハンドリングの強化:** ユーザーに分かりやすいエラーメッセージを表示
4. **コードのリファクタリング:** ターン終了処理を共通化
5. **テストの追加:** 自動テストの実装を検討
