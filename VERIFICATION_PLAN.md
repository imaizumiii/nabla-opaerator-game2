# ロジック検証計画書

## 検証対象ファイル
- `app/components/GameBoard.tsx`
- `app/hooks/useGameState.ts`

---

## 1. カード選択ロジックの検証

### 1.1 関数カードの選択
**テストケース:**
- [x] 何も選択されていない状態で関数カードをクリック → 単一選択される
- [x] 既に選択されている関数カードをクリック → 選択解除される
- [x] 関数カード選択後に別の関数カードをクリック → 新規選択（前の選択は解除）

**期待結果:**
- 関数カードは常に単一選択
- 選択状態が正しく表示される

### 1.2 演算子カードの選択
**テストケース:**
- [x] 微分カードをクリック → 選択される
- [x] 積分カードをクリック → 選択される
- [x] 微分カード選択後に積分カードをクリック → スタックされる（両方選択）
- [x] 微分カード選択後に別の演算子（乗算など）をクリック → 微分は解除、新規選択

**期待結果:**
- 微分/積分はスタック可能
- その他の演算子は単一選択

### 1.3 乗算・除算のオペランド選択
**テストケース:**
- [ ] 乗算カードを選択 → メッセージ表示「関数カードを選択してください」
- [x] 乗算カード選択後に関数カードをクリック → オペランドとして追加
- [x] 既にオペランドが選択されている場合、別の関数カードをクリック → オペランドが入れ替わる

**期待結果:**
- 乗算/除算には関数カード（オペランド）が必要
- オペランドは1枚のみ選択可能

---

## 2. Deploy機能の検証

### 2.1 通常のDeploy
**テストケース:**
- [x] 手札から関数カードを選択し、自分のフィールドをクリック → フィールドに配置される
- [x] 手札から関数カードを選択し、相手のフィールドをクリック → 相手フィールドに配置される
- [x] Deploy後、手札からカードが削除される
- [x] Deploy後、ターンが切り替わる

**期待結果:**
- カードが正しいフィールドに配置される
- 手札から正しく削除される
- ターン管理が正しく動作する

### 2.2 フィールド上限の検証
**テストケース:**
- [x] フィールドに3枚配置済みの状態でDeployを試行 → アラート表示「フィールドに配置できる関数は３枚までです」
- [x] アラート表示後、カードは手札に残る（削除されない）
- [x] ターンは切り替わらない

**期待結果:**
- 3枚制限が正しく動作する
- エラー時は状態が変更されない

### 2.3 重複配置（線形従属）の検証
**テストケース:**
- [x] 既に場に存在する関数カード（例: `x`）を選択してDeploy → 警告ダイアログ表示
- [x] 警告で「キャンセル」を選択 → Deployがキャンセルされる（カードは手札に残る）
- [x] 警告で「OK」を選択 → Deployされるが、線形従属により即座に消滅する（手札からは失われる）

**期待結果:**
- 重複時の警告が表示される
- 確認後の動作が正しい

---

## 3. 演算子適用の検証

### 3.1 単一演算子の適用
**テストケース:**
- [x] 微分カードを選択し、フィールドの関数カードをクリック → 微分が適用される
- [x] 積分カードを選択し、フィールドの関数カードをクリック → 積分が適用される
- [x] 極限カード（∞）を選択し、フィールドの関数カードをクリック → 極限が適用される
- [x] logカードを選択し、フィールドの関数カードをクリック → logが適用される

**期待結果:**
- 各演算子が正しく適用される
- 計算結果が正しくフィールドに反映される

### 3.2 スタック演算子の適用
**テストケース:**
- [x] 微分カード2枚を選択し、`x^2`に適用 → `2`（定数）になる
- [x] 微分カード1枚 + 積分カード1枚を選択し、`x`に適用 → `x`のまま（恒等変換）
- [ ] 微分カード3枚を選択し、`x^3`に適用 → `6`になる

**期待結果:**
- スタックされた演算子が順次適用される
- 計算結果が正しい

### 3.3 乗算・除算の適用
**テストケース:**
- [ ] 乗算カード + 関数カード（例: `x`）を選択し、フィールドの`1`に適用 → `x`になる
- [ ] 乗算カード + 関数カード（例: `x`）を選択し、フィールドの`x`に適用 → `x^2`になる
- [ ] 除算カード + 関数カード（例: `x`）を選択し、フィールドの`x^2`に適用 → `x`になる
- [ ] 乗算カードのみ選択（オペランドなし）で適用を試行 → アラート表示「手札から関数カードを選択してください」

**期待結果:**
- オペランドが必要な演算子が正しく動作する
- 計算結果が正しい

### 3.4 ゼロ関数の検証
**テストケース:**
- [ ] 演算子適用により関数が`0`になる → フィールドからカードが削除される
- [ ] フィールドが空になる → 勝利条件が発動する

**期待結果:**
- `isZero: true`の場合、カードが削除される
- 勝利判定が正しく動作する

---

## 4. AoE演算子（ナブラ/ラプラシアン）の検証

### 4.1 ナブラの検証
**テストケース:**
- [ ] ナブラカードを選択し、フィールドエリアをクリック → フィールドの全関数が1回微分される
- [ ] 相手フィールドにナブラを適用 → 相手フィールドの全関数が微分される
- [ ] 自分のフィールドにナブラを適用 → 自分のフィールドの全関数が微分される

**期待結果:**
- フィールドの全関数に微分が適用される
- ターゲット指定なしで動作する

### 4.2 ラプラシアンの検証
**テストケース:**
- [ ] ラプラシアンカードを選択し、フィールドエリアをクリック → フィールドの全関数が2回微分される
- [ ] `x^2`にラプラシアンを適用 → `2`（定数）になる
- [ ] `x`にラプラシアンを適用 → `0`（削除される）

**期待結果:**
- 全関数に2回微分が適用される
- 計算結果が正しい

### 4.3 AoEの線形従属チェック
**テストケース:**
- [ ] ナブラ適用後、同じ関数が複数存在する場合 → 線形従属により重複が削除される
- [ ] ラプラシアン適用後、線形従属チェックが実行される

**期待結果:**
- AoE後にも線形従属チェックが正しく動作する

---

## 5. エッジケースの検証

### 5.1 空フィールドの検証
**テストケース:**
- [ ] フィールドが空の状態で演算子を適用 → 何も起こらない（またはエラー処理）
- [ ] フィールドが空の状態でDeploy → 正常に配置される

**期待結果:**
- 空フィールドに対する適切な処理

### 5.2 手札不足の検証
**テストケース:**
- [ ] 手札が空の状態 → UIが正しく表示される
- [ ] 手札が0枚の状態でカード選択を試行 → 何も起こらない

**期待結果:**
- 手札が空でもゲームが進行可能

### 5.3 デッキ枯渇の検証
**テストケース:**
- [ ] デッキが空の状態でターン終了 → 手札が7枚未満でもドローされない
- [ ] デッキが空でもゲームは継続する

**期待結果:**
- デッキ枯渇時の適切な処理

### 5.4 勝利条件の検証
**テストケース:**
- [ ] 相手フィールドが空になる → `winner: 'player'`が設定される
- [ ] 自分のフィールドが空になる → `winner: 'opponent'`が設定される
- [ ] 勝利後は操作が無効化される

**期待結果:**
- 勝利条件が正しく判定される
- 勝利後のゲーム進行が止まる

### 5.5 ターン管理のエッジケース
**テストケース:**
- [ ] ターン終了時にデッキが空 → ドローされないがターンは切り替わる
- [ ] 勝利後のターン切り替え → 実行されない

**期待結果:**
- エッジケースでもターン管理が正しく動作する

---

## 6. 線形従属チェックの検証

### 6.1 重複関数の検証
**テストケース:**
- [ ] 同じ式（例: `x`）がフィールドに2枚ある → 線形従属により1枚削除される
- [ ] 正規化後同じ式（例: `2x` と `x`）がフィールドにある → 線形従属により1枚削除される

**期待結果:**
- 正規化により係数の違いを無視して判定される
- 重複が正しく削除される

### 6.2 APIエラーの検証
**テストケース:**
- [ ] 線形従属チェックAPIがエラーを返す → エラー処理が実行される（重複なしとして扱う）
- [ ] APIエラー時でもゲームが継続する

**期待結果:**
- APIエラー時の適切なフォールバック

---

## 7. UI/UXの検証

### 7.1 選択状態の表示
**テストケース:**
- [ ] カード選択時、選択されたカードが視覚的に識別できる
- [ ] ホバー時、カードが浮き上がる
- [ ] メッセージインジケータが正しく表示される

**期待結果:**
- UIが直感的に操作できる

### 7.2 ターン表示の検証
**テストケース:**
- [ ] 現在のターンプレイヤーが正しく表示される
- [ ] ターン数が正しくカウントされる
- [ ] 勝利時に勝者が表示される

**期待結果:**
- ゲーム状態が視覚的に分かりやすい

---

## 検証手順

### 手動検証の場合
1. ブラウザでゲームを起動
2. 各テストケースを順次実行
3. 期待結果と実際の結果を比較
4. 不整合があれば記録

### 自動検証（将来実装）
- Jest + React Testing Libraryを使用
- 各関数のユニットテストを作成
- インテグレーションテストを作成

---

## 検証結果記録

検証実施日: _______________
検証実施者: _______________

### 検証結果サマリー
- 総テストケース数: ___
- 成功: ___
- 失敗: ___
- 保留: ___

### 発見された問題
1. [問題の詳細]
2. [問題の詳細]
