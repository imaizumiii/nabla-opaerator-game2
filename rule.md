# ナブラ演算子ゲーム 公式ルールブック (Ver 2.0)

## 1. ゲームの概要
ナブラ演算子ゲームは、関数空間を舞台にした2人対戦型のカードゲームです。
プレイヤーは「関数（基底）」を場に展開し、「演算子」を駆使して相手の関数を微分・極限操作などで「0（ゼロ）」または「無」にし、相手の存在を消滅させることを目指します。

### 勝利条件
以下のいずれかを満たしたプレイヤーが勝利します。
1. **相手の場の関数カードを全て消滅させる（0枚にする）。**
2. 相手が数学的に不正な操作（ゼロ除算など）を行い、反則負けとなる。
3. 山札が尽きた時点で、場に残っている関数の次元・強さが高い方（判定勝ち）。

---

## 2. コンポーネントとフィールド

### カードの種類
1. **関数カード (Function Card)**
   - プレイヤーの「ライフ」であり「戦力」です。
   - 例: $1, x, x^2, e^x, \sin x, \cos x$
   
2. **演算子カード (Operator Card)**
   - 関数に作用し、状態を変化させるカードです。
   - 例: 微分($d/dx$), 積分($\int$), ナブラ($\nabla$), 極限($\lim$)

### フィールド構成
- **場 (Field)**: 各プレイヤーが関数カードを配置する場所。
  - **上限**: 最大 **3枚** まで。
- **手札 (Hand)**: プレイヤーが保持するカード。
  - **上限**: **7枚**。
- **山札 (Deck)**: カードを引く場所。

---

## 3. ゲームの進行

ゲームはターン制で進行します。

### 1. ドローフェーズ
- ターン開始時、手札が7枚になるまで山札からカードを引きます。

### 2. メインフェーズ
以下のいずれかのアクションを1回行います。

#### A. 関数の展開 (Deploy)
- 手札にある関数カードを1枚、自分の場に出します。
- **制限**: 場の上限は3枚です。既に3枚ある場合は展開できません。
- **線形従属ルール**: 既に場にある関数と同じ関数（係数違い含む）は出せません。

#### B. 演算の実行 (Execute Operation)
- 手札の演算子カードを使用し、場の関数（自分または相手）に適用します。
- 複数の演算子を組み合わせて使用することも可能です（スタック）。
  - 例: $d/dx$ を2枚出して「2階微分」として扱う。
- **全体攻撃**: 「ナブラ($\nabla$)」や「ラプラシアン($\Delta$)」は、相手の場のカード全てに作用します。

### 3. クリンナップフェーズ
演算の結果に基づき、場の状態を更新します。
1. **消滅判定**: 計算結果が $0$、または発散（$\pm \infty$）した関数は場から取り除かれます。
2. **線形従属チェック**: 演算の結果、自分の場に同じ関数が複数存在することになった場合、1つを残して他方は消滅します。
3. ターンを終了し、相手の手番になります。

---

## 4. ルール詳細・システム

### 4.1 係数自由の原則 (Principle of Coefficient Freedom)
ゲームのテンポを重視し、**定数倍の違いは無視**されます。
- $2x$ も $3x$ も、ゲーム上は全て単なる **$x$** として扱います。
- 微分: $x^2 \xrightarrow{d/dx} 2x$ ですが、カードとしては **$x$** に変化します。
- 積分: $x \xrightarrow{\int} \frac{1}{2}x^2$ ですが、カードとしては **$x^2$** に変化します。
- 三角関数: $\sin x \xrightarrow{d/dx} \cos x \xrightarrow{d/dx} -\sin x$ ですが、$-\sin x$ は **$\sin x$** として扱います。

### 4.2 線形従属の解決 (Resolution of Linear Dependence)
同一プレイヤーの場に「数学的に同じ種類」の関数が並ぶことは許されません。
- 衝突例: 場に $x$ がある状態で $x^2$ を微分して $x$ になった場合、片方の $x$ は消滅します。
- **戦略**: 相手の場にある関数と同じ形になるように演算を行い、相手のカードを「衝突消滅」させることが可能です。

### 4.3 主な演算子の効果

| カード名 | 効果 | 対象 |
| :--- | :--- | :--- |
| **微分 ($d/dx$)** | 対象の関数を1回微分する。複数枚出しで高階微分が可能。 | 単体 |
| **ナブラ ($\nabla$)** | 相手の場の **全ての関数** を1回微分する。 | 全体(相手) |
| **ラプラシアン ($\Delta$)** | 相手の場の **全ての関数** を2回微分する。 | 全体(相手) |
| **積分 ($\int$)** | 対象の関数を積分し、次数を上げる。主に防御用。 | 単体 |
| **極限 ($\lim$)** | $x \to \infty, 0$ などの極限をとる。0になれば消滅。 | 単体 |
| **乗算/除算** | 手札の関数カードと組み合わせて使用する（例：$\times x$）。 | 単体 |
| **合成 ($f \circ g$)** | $\log, \sqrt{\quad}$ などを適用する。 | 単体 |

### 4.4 特殊な関数の特性
- **$e^x$**: 微分・積分しても変化しない最強の盾。対数($\log$)や極限($x \to -\infty$)が弱点。
- **$1/x$**: 微分するほど次数が負の方向に増える（$1/x^2, 1/x^3...$）。積分で $\log x$ になる。
- **$\sin x / \cos x$**: 微分で相互に入れ替わる（符号は無視）。極限をとると振動（定義不能）となることが多く、反則勝ちを誘発しやすい。

